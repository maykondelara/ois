# Local development

Copy `.env.example` to `.env.local`, then run `corepack pnpm infra:up`, `corepack pnpm db:generate`, and `corepack pnpm dev`. Use `corepack pnpm infra:down` to stop local dependencies.
